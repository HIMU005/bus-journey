function removeBgById(elementID) {
    const element = document.getElementById(elementID);
    element.classList.remove('bg-btn-color')
}
function setBgByID(element) {
    const element = document.getElementById(elementID);
    element.classList.add('selected-btn');
}